import { ModulePlaceholder } from "@/components/ui/module-placeholder";

export default function ConfiguracoesPage() {
  return (
    <ModulePlaceholder
      eyebrow="Aplicação"
      title="Configurações"
      description="Área reservada às configurações da aplicação."
      message="As opções serão adicionadas somente conforme necessidades reais forem definidas."
    />
  );
}
